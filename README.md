# Solid Start Example with `@ts-rest/solid-query`

This is a simple example of how to use the `@ts-rest` library, specifically the `@ts-rest/solid-query` package!

This is the default solid start template "Hacker News"
